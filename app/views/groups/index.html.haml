= render(partial: 'layouts/nav_bar')

%h2 Groups

- if @groups.any?
  %ul 
    - @groups.each do |group|
      %li
        = link_to group.name, group_path(group)
        %br
- else
  %h4 
    We do not have any groups right now. Please add one!

= link_to 'Create New Group', new_group_path