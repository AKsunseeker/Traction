= render(partial: 'layouts/nav_bar')

%h2 Welcome,
  - if user_signed_in? %>
    = current_realtor.first_name %>

%h2 Catagory

= link_to 'Trainer Create New Category', new_category_path

- if @categories.any?
  %ul
    - @category.each do |category|
      %li
        = link_to @category.name, category_path
      %li
        = link_to 'Edit', edit_category_path(category)
      %li
        = link_to 'Delete', category_path
      