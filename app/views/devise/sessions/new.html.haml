%br
.container
  .row
    .col.s10.offset-s1
      %h2.center Log in
      = form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
        .row
          .field.col.s10.offset-s1
            = f.label :email, class: 'black-text'
            %br/
            = f.email_field :email, autofocus: true, class: 'black-text form-border'
          .field.col.s10.offset-s1
            = f.label :password, class: 'black-text'
            %br/
            = f.password_field :password, autocomplete: "off", class: 'black-text form-border'
        - if devise_mapping.rememberable?
          .field.col.s10.offset-s1
            = f.check_box :remember_me, class: 'black-text'
            = f.label :remember_me, class: 'black-text'
        %br
        .actions.center
          = f.submit "Log in", class: 'btn red darken-4 '
      %br
      .center
        = render "devise/shared/links"